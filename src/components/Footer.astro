---
import site from '../data/site.json';
import Logo from './Logo.astro';
import Icon from './Icon.astro';

const year = new Date().getFullYear();
---

<footer class="footer">
  <div class="container footer__inner">
    <Logo variant="light" />
    <div class="footer__phones">
      <a class="footer__item" href={site.phoneHref}>
        <Icon name="phone" size={18} />
        {site.phone}
      </a>
      <a class="footer__item" href={site.phoneHref2}>
        <Icon name="phone" size={18} />
        {site.phone2}
      </a>
    </div>
  </div>
  <div class="container">
    <p class="footer__meta">
      © {year} АрсБудІнвест (Арс Буд Інвест) — будівництво будинків під ключ,
      бетонні блоки, оренда спецтехніки. Івано-Франківська область.
    </p>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-footer);
    color: var(--color-footer-text);
    padding-block: 28px;
  }

  .footer__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    flex-wrap: wrap;
  }

  .footer__item {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-size: 17px;
    font-weight: 700;
    transition: color 0.18s ease;
  }

  .footer__item svg {
    color: var(--color-orange);
  }

  .footer__item:hover {
    color: #fff;
  }

  .footer__phones {
    display: flex;
    align-items: center;
    gap: 14px 30px;
    flex-wrap: wrap;
  }

  .footer__meta {
    margin-top: 22px;
    padding-top: 18px;
    border-top: 1px solid rgba(255, 255, 255, 0.12);
    font-size: 13px;
    line-height: 1.6;
    opacity: 0.65;
    text-align: center;
  }

  @media (max-width: 600px) {
    .footer__inner {
      flex-direction: column;
      align-items: center;
      gap: 18px;
    }
    .footer__phones {
      flex-direction: column;
      gap: 14px;
    }
  }
</style>
